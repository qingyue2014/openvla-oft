# INVALID — Retired L1-A4 Spatial v4 Review Material

The pre-v5 `formal_*`, `smoke_*`, `er_safe_reference*`, and other historical
L1-A4 spatial review directories under this task directory are invalid for
the current experiment. They belong to the retired v4 or earlier state pools.

The v4 ER/EC target bowl was tilted `15.57–30.99°` at the first policy-visible
frame after the evaluator's 10-step wait. Therefore all associated videos,
outcome labels, metrics, reports, tables, and HTML entries must not be used as
evidence.

Only material explicitly named `L1-A4_v5_*` under these directories belongs
to the repaired scene:

- `v5_initial_policy_frames/`
- `v5_physical_stability/`

The v5 material is scene-validation evidence only. Formal model evaluation
has not started and remains blocked pending explicit human policy-view
approval.
